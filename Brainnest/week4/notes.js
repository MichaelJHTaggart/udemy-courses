//? What is a prototype?
// Create an array out of pure data

const cars = "Mustang, Golf".split(', ');

//? Where does an Array store its data?

//? If an Array stores its data by the index, When does an Array store the data?

let array = ["zero", "one", "two"]
array.unshift("newZero")

//? What happens with the unshift?